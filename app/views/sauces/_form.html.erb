<%= form_with(model: sauce, local: true) do |form| %>
  <% if sauce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sauce.errors.count, "error") %> prohibited this sauce from being saved:</h2>

      <ul>
      <% sauce.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sID %>
    <%= form.text_field :sID %>
  </div>

  <div class="field">
    <%= form.label :sName %>
    <%= form.text_field :sName %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
