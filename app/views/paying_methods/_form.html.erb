<%= form_with(model: paying_method, local: true) do |form| %>
  <% if paying_method.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paying_method.errors.count, "error") %> prohibited this paying_method from being saved:</h2>

      <ul>
      <% paying_method.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :provider %>
    <%= form.text_field :provider %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
